{
  "name": "@studio-render/orchestrator",
  "version": "0.1.0",
  "private": true,
  "description": "Job queue and routing between Fast and Quality render tiers",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "echo no-tests"
  },
  "dependencies": {
    "@studio-render/capture": "workspace:*",
    "@studio-render/render-fast": "workspace:*",
    "@studio-render/render-quality": "workspace:*",
    "@studio-render/video": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
